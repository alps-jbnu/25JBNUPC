# JBNUPC 2025 에디토리얼

## 출제한 사람들

### 전북대학교 ALPS

김담은, 송정규, 이수호, 이인우, 허성범, 황지찬

지도교수: 정종욱 교수님

### 외부 출제위원

정재헌

## 문제 목록

| #    |                             | 출제자 | 예상 난이도 |
| ---- | --------------------------- | ------ | ----------- |
| A    | 비팔로 길들이기             | 송정규 | Easy        |
| B    | 비팔로 산책                 | 이수호 | Easy        |
| C    | 층에서 만나자               | 이수호 | Easy        |
| D    | 부스 둘러보기               | 허성범 | Medium      |
| E    | N각 관계                    | 허성범 | Medium      |
| F    | 성범이의 검증 시험에 답하라 | 이인우 | Medium      |
| G    | 원 사이의 점                | 김담은 | Medium      |
| H    | 순수한 사랑의 주괴          | 허성범 | Hard        |
| I    | 비둘기집 사이클             | 정재헌 | Hard        |

## A. 비팔로 길들이기

태그: 그리디, DP

### 그리디 해법

가정: 연속으로 비가 오지 않는 구간은 모두 같은 행동을 하는 것이 유리하다.

$m$일동안 비가 오지 않을 때

|        | 1    | 2    | 3    | 4    | 5    | 6    | ...  | x       |
| ------ | ---- | ---- | ---- | ---- | ---- | ---- | ---- | ------- |
| 빗질   | 1    | 3    | 6    | 10   | 15   | 20   | ...  | $5x-10$ |
| 달리기 | 3    | 6    | 9    | 12   | 15   | 18   | ...  | $3x$    |

$m\geq{}5$이면 빗질, 아니면 달리기가 유리하다.

구간별 길들임 수치의 합을 구해 출력한다.

### DP 해법

하루마다 "달리기"와 "빗질" 중 하나를 선택하되, 빗질은 연속 횟수에 따라 점수가 올라가므로 $i$번째 날에 $j$번 연속으로 빗질한 최고 점수를 2차원 배열에 저장한다. $(1\leq{}j\leq{}5)$

날씨에 따라 가능한 선택지를 판단하고, 이전 날들의 상태로부터 오늘의 상태로 점수를 최대화하는 방식으로 DP를 구성한다. 이 때 5번 넘게 빗질한 경우에는 5번 빗질한 경우에 덮어쓴다.

## B. 비팔로 산책

태그: 조합론, 브루트포스

### 조합론 해법

각 칸을 방문하는 횟수는 칸에 들어가는 방법과 나오는 방법의 수의 곱이다. $i$열 $j$행을 지나가는 횟수는 아래와 같이 구할 수 있다.

$\binom{i+j}{i}\times{}\binom{14-i-j}{7-i}$
$=\frac{(i+j)!}{i!j!}\times{}\frac{(14-i-j)!}{(7-i)!(7-j)!}$

이렇게 구한 방문 횟수에 각 칸의 행복도를 곱해 더한 값을 출력하면 된다.

### 브루트포스/백트래킹 해법

경로의 수는 '오른쪽' 7개와 '아랫쪽' 7개를 나열한 수열의 갯수와 같다. `std::next_permutation` 등을 사용해 모든 경로를 직접 확인할 수 있다. 총 3432개 경로를 찾을 수 있다.

마찬가지로 재귀 함수를 사용한 백트래킹으로 답을 찾을 수도 있다.

## C. 층에서 만나자

태그: 수학, 브루트포스

수호가 엘리베이터에 타기 위해 담은과 만나는 층을 $K$라 하자. $K$는 $N$과 $M$ 사이의 임의의 값을 가질 수 있다. 가능한 모든 $K$에 대해 이동시간을 구하면 가장 작은 값이 답이 된다.

수호가 $K$층으로 이동하는 데에는 $T_1 = 3|K-N|$ 만큼의 시간이 걸린다.

담은이 $K$층으로 이동하는 데에는 $T_2 = |K-M|$ 만큼의 시간이 걸린다.

두 사람이 $K$층에서 만날 때까지는 $max(T_1,T_2)$의 시간이 걸린다.

두 사람이 만난 후에는 1층까지 엘리베이터를 타고 이동하는 것이 최선이므로, 총 소요되는 시간은 $max(T_1,T_2) + K - 1$ 이다.

### 노트

수의 범위가 크다면 이분 탐색을 통해 답을 구할 수도 있다.

## D. 부스 둘러보기

태그: 조합론, 많은 조건 분기

### 마지막 글자

마지막 글자는 $t[1..n]$부터 $t[n..n]$까지 모든 부분 문자열에 포함되기 때문에 마지막 글자부터 보면 유리하다.

- $t[n]=1$이라면 답은 존재하지 않는다. 0을 출력하고 프로그램을 종료하면 된다.
- $t[n]=0$이라면 $s[n]=1$이다.

### 나머지 글자

임의의 $1\leq{}i\leq{}n-1$에 대해

- $t[i]=1$이라면 $s[i]=1$이다.

- $t[i]=0$이라면 $s[i]$는 $0$ 또는 $1$이다. 

마지막이 아닌 글자 중 $t[i]=0$인 갯수를 $m$이라 하면 답은 $2^m$이다.

## E. N각 관계

태그: 그래프 탐색

서로 연결된 노드의 집합을 부분 그래프로 나누었을 때, 각 부분 그래프에는 반드시 하나의 사이클이 존재한다.

그래프 탐색을 통해 각 그래프의 사이클의 크기를 구할 수 있다. 그 중 가장 큰 값을 출력한다.

탐색 중 이미 방문한 노드를 발견한 경우 이미 처리한 사이클이므로 빠져나와야 한다.

## F. 성범이의 검증 시험에 답하라

태그: 분리 집합(Union-find), 정렬, 그래프 이론

간선으로 연결된 부분 그래프를 분리 집합을 사용해 관리한다. 간선이 연결될 때마다 부분 그래프를 합친다.

부분 그래프마다 포함된 정점들의 목록을 저장한다. 합병할 때마다 정점 목록을 내림차순으로 정렬한 뒤 가장 큰 20개만 남긴다.

## G. 원 사이의 점

태그: 수학, 이분 탐색, 정렬

각 점이 어떤 원 안에 있는지는 원의 방정식$x^2+y^2=ir^2$을 사용해 판정할 수 있다. 점들을 분류하는 여러 가지 방법이 있는데, 아래는 각각 사용 가능한 방법들이다.

- 이분 탐색으로 점을 포함하는 가장 작은 원을 찾는다.
- 모든 점을 원점으로부터의 거리를 기준으로 정렬하면 앞쪽 점은 뒷쪽 점보다 작거나 같은 원에 포함된다. 원의 갯수가 많지 않으므로 작은 원부터 순차적으로 탐색한다.

분류를 마치면 각 원마다 포함된 점의 수를 구할 수 있다. 가장 점이 많은 원을 출력한다.

풀이 과정에서 int 오버플로우가 발생할 수 있으므로 64비트 정수형을 사용하는 것이 좋다. 또한 제곱근을 사용하는 것보다 거리의 제곱을 그대로 사용하면 안전하다.

## H. 순수한 사랑의 주괴

태그: 비트마스크, 그리디, 이분 탐색

### 가정

가정: 구간 $[L,R]$에 속하는 수가 3개 이상이면 popcount가 1인 해가 반드시 존재한다.

증명: 어떤 짝수 $x$와 홀수 $x+1$의 popcount는 1이다. 3개 이상인 연속된 정수들 중에는 가장 큰 수가 아닌 짝수가 존재한다.

가정이 참이므로

- $L=R-1$이면 답은 $L⊕R$이다.
- 그렇지 않으면 popcount가 1인 답이 존재하고, popcount가 2 이상인 해를 조사할 필요가 없다.

### popcount==1

모든 비트에 대해 $L$ 이상이고 $i$번째 비트가 $0$인 가장 작은 정수 $a$를 찾는다. 그러면 $i$번째 비트만 다른 정수 쌍 $(a,a+2^i)$가 구간 $[L,R]$에 포함되는지 확인할 수 있다.

시간복잡도: $O(\log R)$

## I. 비둘기집 사이클

태그: 희소 배열, 그래프 탐색, 함수형 그래프

$i$번째 비둘기집에 있던 비둘기들이 $x$년 뒤에 어디에 배치되는지 나타내는 배열을 $F^x$로 쓸 수 있다. 쿼리를 처리해도 $F$는 바뀌지 않으므로 $F^x$도 바뀌지 않는다.

- 그래프 탐색 또는 희소 배열(스파스 테이블)을 사용해 $F^{10^6}$를 구한다.
- $A$와 $F^{10^6}$을 사용해 $10^6$년 뒤 비둘기집의 상태를 구한다.
- 위에서 구한 값들을 사용해 쿼리를 처리한다.

### 전처리: 그래프 탐색

그래프의 모든 간선의 방향을 뒤집어 역방향 그래프를 만든다. 원본 그래프의 각 사이클마다 사이클 위의 임의의 노드 $j$를 출발점으로 BFS로 도달 가능한 노드들의 거리를 구한다.

$j$로부터의 거리가 값이 같은 점들은 $x\geq n$인 임의의 시간 $x$에 같은 노드에 도달한다. 이 때 도착점은 $j$에서 출발해 $(10^6-d)\mod{l}$만큼 정방향으로 진행한 점이다.

시간복잡도: $O(n)$

### 전처리: 희소 배열

문제에 주어진 $F$와 같은 형태인 그래프를 함수형 그래프라고 한다. 임의의 양의 정수 $x, y$에 대해 $F^x[i]=F^{x-y}[F^y[i]]$가 성립한다.

$F^{2x}[i]=F^{x}[F^{x}[i]]$이므로, $10^6$보다 작은 모든 $2$의 거듭제곱 $m$에 대해 $F^m$을 전처리할 수 있다. 이들을 합성하면 $F^{10^6} [i]$를 찾을 수 있다.

시간복잡도: $O(n \log n)$

### 쿼리 처리

get 쿼리를 받을 때마다 도착점의 값을 출력한다.

set 쿼리를 받을 때마다 테이블에서 도착점을 찾아 기존 $A[i]$만큼 빼고 새로운 값만큼 더한다.

시간복잡도: 쿼리 당 $O(1)$
